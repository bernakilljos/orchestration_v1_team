---
description: 바로 실행 가능한 결과물(코드/HTML/앱) 생성 — 완전하고 즉시 동작하는 형태로
---

Create a **complete, runnable artifact** for the following request.

Requirements:
- Fully self-contained — no missing pieces, no "fill in your API key here" placeholders
- Immediately runnable as-is
- Include all imports, dependencies, and setup instructions in a comment at the top
- Production-quality: error handling, edge cases handled
- If it's UI: make it visually clean, not just functional
- Output the complete file(s), nothing truncated

What to build: $ARGUMENTS
