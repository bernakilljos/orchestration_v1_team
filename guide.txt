============================================================
  Orchestration Kit v1 — Complete Guide
  Claude + Codex + Gemini Multi-AI Framework
============================================================


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  1. 설치
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  [3 가지 모드] — 라디오 버튼 또는 인자로 선택
    ⦿ Full Orchestration   Claude + Codex + Gemini (추천)
    ○ Codex 단독           Claude 없이 Codex 만 사용
    ○ Gemini 단독          Claude 없이 Gemini 만 사용

  [방법 A] setup.exe — GUI 위자드 (추천)
    1. https://github.com/bernakilljos/orchestration/releases
    2. OrchestrationKit-Setup.exe 다운로드
    3. 더블클릭 → 위자드에서 모드 라디오 선택 → 경로 → 설치
    4. 완료 후 모드별 자동 실행 옵션 (Claude / codex-go / gemini-go)

  [방법 B] git clone — CLI 인자로 모드 지정
    git clone https://github.com/bernakilljos/orchestration.git
    cd orchestration
    setup\setup.bat        C:\work\myproject       ← Full (기본)
    setup\setup.bat full   C:\work\myproject       ← Full (명시)
    setup\setup.bat codex  C:\work\myproject       ← Codex 단독
    setup\setup.bat gemini C:\work\myproject       ← Gemini 단독
    setup\setup.bat anl    C:\work\myproject       ← Full + 소스 분석

    또는 install.bat 직접:
      install.bat              C:\work\myproject  ← Full
      install.bat codex        C:\work\myproject  ← Codex 단독
      install.bat gemini       C:\work\myproject  ← Gemini 단독

  [방법 C] 사일런트 — 자동화/스크립트용
    OrchestrationKit-Setup.exe /VERYSILENT /DIR="C:\work\myproject"
    OrchestrationKit-Setup.exe /VERYSILENT /TYPE="codex_only"  /DIR="..."
    OrchestrationKit-Setup.exe /VERYSILENT /TYPE="gemini_only" /DIR="..."

  [방법 D] 독립형 설치 스크립트 (v1) — git 저장소에서 직접
    Codex 단독:  install_codex.bat C:\work\myproject
    Gemini 단독: install_gemini.bat C:\work\myproject
    → .env.example 자동 생성, 토큰 로그 기능, 상세 가이드 포함
    → install_codex/gemini.bat 는 PowerShell wrapper (한글 깨짐 방지)

  [방법 E] Team 동기화 (인프라 본체 → 팀 폴더)
    bash .claude/scripts/sync-to-team.sh
    또는 슬래시: /sync-team
    → orchestration_v1 → ../orchestration_v1_team 자동 복사
    → 시크릿 (docs/ini/), 상태 (.claude/state/), 락 제외

  [모드별 사용 흐름]
    Full   →  cd <target>  →  claude
    Codex  →  cd <target>  →  codex-go "회원가입 페이지 만들어줘"
    Gemini →  cd <target>  →  gemini-go "이 코드 검증해줘"

  [원격 24/7 운영 — 4주차 (신규 2026-05-07)]
    PC 꺼져 있어도 클라우드에서 작동시키려면:
      claude → /exec_remote-setup    Oracle Cloud Free Tier (4 OCPU·24GB 무료)
      claude → /exec_remote-ssh      SSH 키 ED25519 + ~/.ssh/config 자동
      claude → /exec_remote-deploy   bootstrap-vps.sh + 우리 킷 clone
      claude → /exec_remote-mobile   Termius 등 스마트폰 원격 (iOS·Android)
    상세: docs/2026-05-07/4주차-원격제어.md · plugins/exec_remote/README.md

  [서비스만 재시작]
    install.bat restart

  [무인 진행 — 사용자 입력 0번]
    install.bat 의 모든 prompt 자동화 (timeout default):
      - 체크포인트 1/3, 2/3: 3초 후 자동 진행
      - Node.js / codex / gemini 설치: 5초 후 자동 Y
      - Claude 실행: 10초 후 자동 N
      - 종료 pause: 3초 후 자동 닫기
    test 우회: SKIP_ELEVATION=1 환경변수 (admin 검사 건너뜀)

  [PAT 자동 처리 — install/setup 양쪽]
    우선순위:
      1. 환경변수 GITHUB_PERSONAL_ACCESS_TOKEN
      2. docs/ini/github.ini 의 GITHUB_PAT=
      3. 둘 다 없으면 docs/ini/ 폴더 + placeholder 자동 생성 + 안내
    PAT 발급: https://github.com/settings/tokens (scope: repo + workflow)
    docs/ini/ 는 .gitignore (시크릿 보호)


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  2. 설치 후 필수 설정 (수동)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  API 키 (CMD에서 실행, 설정 후 새 터미널 열기):
    setx ANTHROPIC_API_KEY "sk-ant-..."
    setx OPENAI_API_KEY    "sk-..."
    setx GEMINI_API_KEY    "AI..."

  확인:
    echo %ANTHROPIC_API_KEY%

  * 나머지(MCP, 플러그인, 설정)는 전부 자동 설치됨


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  3. 기본 사용법
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  [일반 사용 — Claude에게 말하면 됨]
    "로그인 페이지 만들어"
    → Claude가 설계 → task-instruction.md 작성
    → Codex가 구현 → Claude가 보완 → Gemini가 검증

  [명칭 정리 — 헷갈리지 말 것]
    codex-a      단일 태스크 실행 (태스크 파일 1개 처리)
    codex-auto   병렬 구현 워커  (기본 4개 동시 실행)
    gemini-a     단일 검증 실행
    gemini-auto  병렬 검증 워커  (기본 2개)
    claude-auto  Claude 병렬 워커 (기본 3개)

  [수동 실행]
    codex-a --auto           태스크 1개 구현
    codex-auto               4개 워커로 병렬 구현 (기본값)
    codex-auto 10            10개 워커
    haiku-auto               2개 검증 워커 (기본값, Prompt caching)
    haiku-auto 4             4개 Haiku 병렬 검증
    gemini-a --verify        단일 검증 (초장문/멀티모달)
    gemini-auto              2개 Gemini 검증 워커
    claude-auto              3개 Claude 워커 (기본값)

  [중단 — /loop-stop 사용 권장]
    /loop-stop               즉시 프로세스 종료 + 플래그 설정
    수동: echo stop > .claude\tasks\stop


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  4. AI 역할 분담
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ┌──────────┬───────────────────────────────────┐
  │    AI    │              역할                 │
  ├──────────┼───────────────────────────────────┤
  │ Claude   │ 팀장: 설계, 판단, 승인, 보완      │
  │ (Opus 4.7)│ + 문서/기획/디자인 (Gamma, Canva)  │
  ├──────────┼───────────────────────────────────┤
  │ Codex    │ 구현: 500줄+ 코드 1차 구현        │
  │          │ codex-a --auto로 실행              │
  ├──────────┼───────────────────────────────────┤
  │ Haiku 4.5│ 검증: 기본 검증자 (Prompt caching)│
  │ (기본)   │ haiku-auto로 실행 (2개 병렬)      │
  ├──────────┼───────────────────────────────────┤
  │ Gemini   │ 검증: 초장문/멀티모달 (>500k 토큰)│
  │ (Flash)  │ gemini-a --verify로 실행           │
  └──────────┴───────────────────────────────────┘

  [Advisor 모드]
    claude-auto 실행 시 자동 적용:
      실행 모델: claude-sonnet-4-6 (빠르고 저렴)
      조언 모델: claude-opus-4-6 (복잡한 판단)
    수동: claude --model claude-sonnet-4-6 --advisor claude-opus-4-6

  [exec_orch vs exec_claude — 두 코어 플러그인 분담]
    exec_orch    "어떤 AI 가 할 것인가" — 멀티AI 라우팅·워커·핸드오프
    exec_claude  "Claude 는 어떻게 잘 할 것인가" — 구조화 질문·아티팩트·확장 사고
    근거: docs/2026-04-23/claude-vs-orchestration-comparison.md
    다이어그램: docs/screens/arch/claude-mindmap-eating-everything-hassid.png

    exec_claude 커맨드 (5개):
      /claude-status        가용성 + 시나리오 한 화면 요약
      /claude-ask <topic>   구조화 질문 폼 (자유텍스트 대신 선택지)
      /claude-artifact      인터랙티브 HTML (대시보드·계산기·차트·폼·게임)
      /claude-connectors    Slack/Notion/Drive 등 1-click 인증 흐름
      /claude-thinking      Extended Thinking — 복잡 추론 8단계 분해


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  5. 파이프라인
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  [표준 파이프라인]
    요청 접수
      → init (스택 감지, 폴더 생성)
      → research (파일 탐색, 리스크 식별)
      → team-lead (task-instruction.md 작성)
      → Codex 1차 구현 (codex-a --auto)
      → Claude 보완/고도화
      → Gemini 검증 (gemini-a --verify)
      → Claude 채택 → 배포

  [AI Handoff — 강제 인수인계]
    Claude → Codex:  task-instruction.md + handoff-log.md 필수
    Codex → Claude:  implementation-report.md 필수
    Claude → Gemini: verify-*.md + 검증 포인트 필수
    Gemini → Claude: review-result.md (합격/불합격) 필수
    ※ 산출물 없으면 다음 단계 차단 (hook-08)

  [워커 종료 조건 — 3중]
    1. .claude\tasks\stop 파일 → 즉시 종료
    2. heartbeat 5분 미갱신 → Claude 종료 감지 → 종료
    3. 태스크 없이 1시간 대기 → 자동 종료


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  6. 설치되는 것 — 전체 목록
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  [Setup 모듈 — 14단계 (현행 2026-05-07)]
    01 core             .claude 폴더 + CLAUDE.md 복사
    02 defender         Windows Defender 예외
    03 settings         Claude 글로벌 설정 + PS UTF-8
    04 commands         글로벌 명령어 9개:
                          codex-a / codex-auto / codex-auto-global
                          gemini-a / gemini-auto / gemini-auto-global
                          claude-auto / haiku-auto / gemini-verify
                        ※ haiku-auto 는 CLAUDE.md §3.2 기본 검증자
                        ※ *-global 은 멀티 프로젝트 전역 큐 (~/.claude/orca/)
    05 services         status-push, remote-agent 등록
    06 prereqs          Node.js, Claude Code, Cloudflared
    07 github           Git 초기화 + GitHub repo 생성 (v1/team 자동 분기)
    08 plugins          외부 커뮤니티 플러그인 + superpowers 자동 설치
    09 finalize         init, npm install, Claude 실행
    10 video-restore    CodeFormer + Real-ESRGAN
    11 media-enhance    오디오/PDF/PPT 의존성
    12 kit-sync         plugins/ to .claude/ 자동 동기화 + 스키마 검증
    13 init-state-db    SQLite state DB 초기화 (워커·태스크·quota·budget)
    14 mcp-figma        ClaudeTalkToFigma MCP 등록

  [플러그인 — 8개]
    claude-md-management     CLAUDE.md 품질 관리
    code-review              PR 코드리뷰
    commit-commands          git commit/push/PR
    superpowers              TDD + 계획 + 리뷰 자동화
    ui-ux-pro-max            UI/UX 디자인 고급
    everything-claude-code   올인원 확장
    awesome-claude-code      커뮤니티 베스트
    get-shit-done            실행 중심 워크플로우

  [MCP 서버 — 7개 (자동 설치)]
    context7       라이브러리 공식 문서 조회
    playwright     브라우저 자동화
    thinking       단계별 추론
    gemini         Gemini API 직접 호출
    excel          Excel/CSV 읽기·쓰기
    n8n            워크플로우 자동화
    light-rag      경량 문서 검색/참조

  [MCP 서버 — 6개 (claude.ai 로그인 후 자동 연결)]
    Figma, Gamma, Gmail, Google Calendar,
    Hugging Face, Mermaid Chart, Canva

  [토큰 최적화 환경변수 — install.bat 자동 설정]
    CLAUDE_CODE_MAX_THINKING_TOKENS=10000   thinking 과다 소비 차단
    CLAUDE_AUTOCOMPACT_THRESHOLD=50         컨텍스트 50%% 시 자동 압축
    CLAUDE_CODE_SUBAGENT_MODEL=haiku        서브에이전트 저비용 모델 사용

  [MCP 토큰 최적화 — Deferred Tools (자동 적용)]
    Claude Code 최신 버전부터 MCP 도구 스키마를 지연 로딩(Lazy Load)함.

    문제:  MCP 서버가 많아지면 매 턴마다 수만 토큰이 컨텍스트에 자동 주입
           → 비용 폭증 + 컨텍스트 조기 소진 ("MCP 토큰 폭발")

    해결:  Deferred Tools 방식
           - 세션 시작 시 도구 이름만 system-reminder에 나열 (스키마 없음)
           - 실제 호출 전 ToolSearch("select:도구이름")로 스키마 fetch
           - 사용하지 않는 도구는 토큰 0 소비

    효과:  매 턴 MCP 토큰 5,000~20,000+ → ~100 (이름만)
           컨텍스트 수명 대폭 연장, API 비용 절감

    * Claude Code에 기본 내장 — 별도 설치/설정 불필요

  [스킬 — 38개]
    #  이름                 용도
    ── ──────────────────── ──────────────────────────
    01 research             파일 탐색, 리스크 식별
    02 implement            코드 구현 (codex-auto 연동)
    03 review               코드 리뷰 (gemini-auto 연동)
    04 context-summary      500줄+ 파일 요약
    05 deploy               EC2 배포
    06 test                 테스트 자동화
    07 rollback             배포 롤백
    08 design               디자인 에셋 생성
    09 memory-reset         세션 메모리 저장
    10 quality-verify       성능/품질 검증 (4영역)
    11 personas             8개 전문가 페르소나
    12 domain-detect        스택 자동 감지
    13 parallel-dispatch    병렬 디스패치
    14 auto-detail          짧은 요청 자동 확장
    15 theme-factory        테마/디자인 시스템 생성
    16 brand-guidelines     브랜드 가이드라인 관리
    17 debugging-canvas     버그 시각적 분석 (Mermaid)
    18 web-artifacts        HTML/CSS 미리보기 생성
    19 skill-creator        새 스킬 자동 생성 (메타)
    20 claude-seo           SEO 분석/최적화
    21 marketing            마케팅 콘텐츠 생성
    22 remotion             React 프로그래매틱 동영상
    23 owasp-security       OWASP Top 10 보안 감사
    24 ai-handoff           멀티AI 강제 인수인계
    25 media-enhance        미디어 화질·음질 AI 개선
    26 file-protection      핵심 파일 보호 (config.py 등)
    27 mandatory-verify     구현 후 필수 검증 강제
    28 changelog            git log → CHANGELOG.md 자동 생성
    29 api-tester           API 엔드포인트 자동 테스트
    30 docker               Dockerfile + docker-compose 생성
    31 i18n                 다국어 번역/관리 (ko/en/ja/zh)
    32 db-migration         DB 스키마 마이그레이션 관리
    33 github-actions       CI/CD 파이프라인 자동 생성
    34 code-docs            JSDoc/Sphinx/Javadoc 자동 생성
    35 performance-profiler 런타임 성능 분석
    36 data-viz             데이터 차트/대시보드 생성
    37 error-tracker        에러 수집/분류/분석 (Sentry 대체)
    38 token-watchdog       토큰 감시 + 자동 재가동

  [훅 — 11개]
    00 init                  스택 감지, 폴더 생성
    01 pre-task              태스크 등록, 파일 잠금
    02 post-impl             구현 후 품질 게이트
    03 post-review           리뷰 후 학습 반영
    04 pre-deploy            배포 전 최종 점검
    05 post-deploy           배포 후 알림
    06 notify                알림 (성공/실패)
    07 layout-lock           레이아웃 파일 보호
    08 ai-handoff            인수인계 산출물 강제 검증
    -- protect-critical-files config.py/settings.json 수정 차단
    -- post-impl-verify      구현 후 문법/인코딩/보호파일 자동 검증

  [슬래시 커맨드 — 25개 (프로젝트 12 + 모드 13)]
    /check-agents          codex-auto / gemini-auto 가용 여부 확인
    /check-services        status-push / remote-agent 상태 확인
    /vibe-loop             멀티에이전트 루프 시작
    /orcauto-start         워커 자동 시작 활성화
    /orcauto-stop          워커 자동 시작 비활성화
    /loop-stop             실행 중인 모든 루프 중단

    [원격 운영 — exec_remote (4주차, 신규)]
    /exec_remote-setup     Oracle Free Tier 가입 + VM 생성 가이드
    /exec_remote-ssh       SSH 키 ED25519 + ~/.ssh/config 등록
    /exec_remote-deploy    VPS 부트스트랩 + 우리 킷 배포
    /exec_remote-mobile    스마트폰 (iOS·Android) 접속 가이드
    /exec_remote-tmux      tmux 영속 세션 패턴
    /exec_remote-status    VPS 상태 한 화면 (핑·CPU/RAM·heartbeat)

    [모드 커맨드 — 13개 (커뮤니티 제작, 글로벌 설치)]
    /godmode               제한 없이 공격적·강력하게 실행 (aggressive mode)
    /devil                 악마의 변호인 — 모든 가정에 반론 제기
    /10x                   10x 엔지니어 모드 — 핵심만, 최대 효율
    /pitch                 투자자 피치덱 스타일로 정리
    /ghost                 AI 글쓰기 패턴 제거 → 인간처럼 재작성
    /compare               두 옵션 냉정 비교 분석
    /scout                 정찰 모드 — 빠른 정보 수집
    /artifacts             바로 실행 가능한 결과물 생성
    /ooda                  OODA Loop 프레임으로 상황 분석
    /critique              칭찬 없는 냉정한 전문가 비판
    /explainlikeim5        5살도 이해하게 쉽게 설명
    /brief                 핵심만 3줄 이내로
    /teacher               단계별 교육 방식으로 설명

  [에이전트 — 6개]
    01 team-lead     설계/지시서 작성
    02 implementer   구현 실행
    03 reviewer      코드 리뷰
    04 architect     아키텍처 설계
    05 monitor       서버 헬스 체크
    06 designer      디자인 참조/생성

  [도구 — 2개]
    tools/video-restore.py     동영상/이미지 복원 (CodeFormer+Real-ESRGAN)
    tools/media-enhance/       미디어 종합 개선 (동영상/오디오/이미지/PDF/PPT)


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  7. 24시간 자동화 (Phase 1+2 신규)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  [초기 설정]
    1. SQLite 상태 DB 초기화:
       python .claude/scripts/init-state-db.py
       → .claude/state/orca.db 생성 (8 테이블)
       → 기존 token-usage.jsonl 자동 마이그레이션

    2. 일일 예산 상한 설정 (선택):
       python .claude/scripts/route.py --set-daily-limit 50
       → 하루 $50 넘으면 자동으로 breaker 발동 (신규 태스크 시작 중단)

    3. Watchdog 시작 (백그라운드 감시자):
       .claude/scripts/watchdog-start.bat
       → 워커 heartbeat 2분마다 체크
       → 죽은 워커 자동 부활 (quota 소진 시 지수 backoff: 10m→20m→40m→2h)

  [새 검증 워커 — Haiku 4.5]
    기본 검증자가 Gemini → Haiku 로 바뀜. Prompt caching 으로 90% 비용 절감.

    사용법: (기존 gemini-auto 와 동일 패턴)
      haiku-auto              # config 기본 2개 워커
      haiku-auto 4            # 4개 병렬

    언제 Gemini를 쓰나:
      - 태스크 크기 > 500k 토큰 (Haiku 200k 컨텍스트 초과)
      - 멀티모달 (이미지·비디오) 검증
      - Haiku quota 소진 (자동 fallback)

  [라우팅 정책 — Claude Opus 4.7 우선]
    기본 라우터가 4.7 중심으로 재설계됨:
      설계·리팩토링·애매한 태스크 → Opus 4.7 + Extended Thinking (1M context)
      단순 구현 < 200줄 → Sonnet 4.6 (저비용)
      대량 구현 > 800k 토큰 → Codex (병렬 4개)
      검증 → Haiku 4.5 (기본), 500k+ 초장문/멀티모달만 Gemini Flash

    자동 fallback:
      Claude quota 소진 → Codex
      모든 quota 소진 → WAIT (가장 빠른 복구까지 지수 backoff)

  [상태 조회]
    python .claude/scripts/route.py --status
    → Budget: $2.00 / $50.00 (4.0%) - OK
    → Quota: All OK (또는 EXCEEDED - resume in 2h 15m)

    python .claude/scripts/metrics-report.py --hours 24
    → AI별 호출수·성공률·토큰·비용·레이턴시 표

    브라우저:
      http://localhost:8787/api/metrics?hours=24  (JSON)
      http://localhost:8787                        (대시보드, 30초 자동 갱신)

  [Quota 수동 해제]
    python .claude/scripts/route.py --clear-quota claude-opus
    python .claude/scripts/route.py --clear-quota codex

  [Breaker 리셋 (긴급)]
    python .claude/scripts/route.py --reset-breaker

  [상세 문서]
    - docs/caching-strategy.md    — Prompt caching 5분/1h TTL 전략
    - docs/routing-policy.md      — 라우팅 결정 트리 상세
    - docs/metrics-guide.md       — 메트릭 DB 스키마 + 쿼리
    - docs/upgrade-notes-2026-04-23.md  — v1 업그레이드 체크리스트


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  8. MCP 도구 추가 (로그인 협조 필요 시)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  [카테고리별 설치 명령]
    /plug_design     Canva·Mermaid·Figma·PowerPoint (PDF 생성)
    /plug_dev        GitHub·Docker·AWS·Firebase
    /plug_data       PostgreSQL·SQLite·MongoDB (로컬 기반)
    /plug_web        Playwright·Puppeteer·Fetch API
    /plug_collab     Slack·Notion·Jira·Trello·Telegram·Gmail·Calendar (3주차 알림)
    /plug_docs       PDF·DOCX·OCR (로컬 CLI 기반)
    /plug_media      Whisper·TTS·FFmpeg (로컬 CLI 기반)
    /plug_all        위 7개 순차 실행

  [인증이 필요한 도구와 개발자 콘솔]
    Canva      → https://www.canva.dev/api/
    GitHub     → https://github.com/settings/tokens (Personal Access Token)
    Figma      → https://www.figma.com/developers/api
    Notion     → https://www.notion.so/my-integrations (Internal Integration)
    Slack      → https://api.slack.com/apps (OAuth Tokens)
    Google API → https://console.cloud.google.com (Service Account)
    Firebase   → https://console.firebase.google.com

  [환경변수 설정 (OAuth/API Keys)]
    Windows:   set CANVA_API_KEY=<token> && set GITHUB_TOKEN=<token>
    또는        .env 파일에 작성 (gitignore)
    인증 모드:   `claude mcp add -s user <name> -- npx -y <package>`
                 (설치 중 브라우저로 로그인 가능)

  [Windows 필수 조건]
    - cmd /c npx 래퍼 사용 (PowerShell 호환성)
    - 예: `cmd /c npx -y @modelcontextprotocol/server-github`
    - 환경 설정: C:\Users\<NAME>\.claude.json의 mcpServers 편집

  [설치 확인]
    claude mcp list              (설치된 MCP 서버 목록)
    claude mcp remove <name>     (제거 필요 시)

  [트러블슈팅]
    - "failed to connect": cmd /c 래퍼 누락 → 해당 /plug_* 재실행
    - "404 Not Found": npm 패키지 이름 변경됨 → docs/upgrade-notes 확인
    - "EACCES: permission denied": 권한 부족 → Windows 다시시작 후 재시도
    - OAuth 실패: 환경변수 잘못됨 → claude mcp remove 후 재설치


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  10. 대시보드
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  시작:
    dashboard.bat    → http://localhost:8787 + cloudflared 터널

  기능:
    - 멀티 PC 실시간 상태 모니터링
    - 원격 화면 캡처 + 리모트 컨트롤
    - 태스크 상태 (대기/실행/완료)
    - 원격 태스크 전송
    - RDP 원클릭 연결

  통신 방식 (자동 선택):
    1순위: cloudflared 터널 → HTTP 직접 통신 (실시간)
    2순위: git push fallback (30초 간격, 방화벽용)


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  11. 개발 규칙
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  Frontend:  Vue 2.x — optional chaining(?.) 금지
  Backend:   Spring Boot 2.x / Node.js Express
  DB:        MSSQL / MySQL / Oracle
  Alert:     mapActions("alert",[ADD_ALERT])
  하드코딩:  금지 (process.env 사용)
  주석:      "주인" 단어 금지

  [수정 가능]
    *.vue *.jsx *.tsx *.js *.ts *.css *.html
    *.java *.cs *.py *.go *.yml *.json

  [수정 금지 — 제안만]
    *.sql, DDL, migration → docs\query-suggestions.md

  [AI 잠금]
    layouts/, components/ 구조 → hook-07이 자동 보호


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  11. 트러블슈팅
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  [한글 깨짐]
    → install.bat 재실행 (UTF-8 자동 적용)

  [codex-a / gemini-a / haiku-auto / *-global 못 찾음]
    → install.bat 또는 setup.exe 재실행 (모듈 04 가 %APPDATA%\npm 으로 복사)
    → 임시: .claude\scripts\<이름>.bat 직접 실행
    → 2026-05-07 이전 빌드는 haiku-auto / codex-auto-global / gemini-auto-global
      누락 가능 — git pull 후 재설치 필요

  [MCP 서버 누락]
    → claude mcp list로 확인
    → 수동: claude mcp add -s user context7 -- npx -y @upstash/context7-mcp

  [MCP 토큰 폭발 — 컨텍스트가 빠르게 줄어드는 경우]
    → Deferred Tools는 자동 적용됨 (Claude Code 최신 버전)
    → claude --version으로 최신 버전 확인
    → MCP 서버 수 줄이기: claude mcp remove {불필요한서버}
    → 자주 쓰는 MCP만 남기고 나머지 제거 권장

  [hook 에러 — "No such file"]
    → .claude\settings.json에서 없는 hook 파일 참조 제거
    → 최신 settings.json: git pull로 업데이트

  [워커가 안 꺼짐]
    → .claude\tasks\stop 파일 생성: echo stop > .claude\tasks\stop
    → 또는 /loop-stop 명령

  [대시보드에서 PC가 안 보임]
    → wscript "%USERPROFILE%\.claude\status-push-silent.vbs"
    → 또는 install.bat restart

  [설치 로그 확인]
    → type %TEMP%\orchestration-install.log
    → type %TEMP%\orchestration-setup.log


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  12. 플러그인 구조 (plugins/)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  각 플러그인 = commands/ + skills/ + agents/ + hooks/ + codex/ + rules.md
  파이프라인: Hook → Planner → Executor → Validator → State → Retry

  [현재 플러그인 목록 — 26개 (16 stable + 10 spec-only, _template 제외)]
    [Stable — 즉시 사용 가능]
    exec_orch          오케스트레이션 핵심 (pipeline.md 참조)
    exec_remote        원격 24/7 운영 — VPS·SSH·tmux·모바일 (4주차 신규 ⭐)
    exec_claude        Claude 전용 심화 (질문폼·아티팩트·확장사고)
    exec_voice         음성 STT·TTS·회의록·음성 명령
    exec_learning      세션 학습·실패 패턴·최적화 규칙
    exec_session_guard 세션 끊김 대비 스냅샷 자동 저장
    design_ppt         PPT (Claude→Canva→Mermaid→Figma)
    design_excel       Excel (Claude→openpyxl→차트→Sheets)
    design_word        Word (Claude→python-docx→Mermaid→PDF)
    review_qa          코드 리뷰·보안·품질·테스트
    mcp_dev            개발 MCP 설치 (GitHub·Docker·AWS·Vercel)
    mcp_data           데이터 MCP 설치 (PostgreSQL·MongoDB·BigQuery)
    mcp_collab         협업 MCP (Slack·Notion·Jira·Trello·Telegram·Gmail·Calendar)
    mcp_web            웹자동화 MCP (Playwright·Puppeteer)
    mcp_docs           문서처리 MCP (PDF·DOCX·OCR)
    mcp_media          미디어 (Whisper·TTS·FFmpeg)

    [Spec-only — 향후 구현]
    exec_offline / exec_scheduler / ai_rag / cost_youtube
    design_pdf / design_video / design_web / mcp_queue / mcp_social / music_studio

  [접두사 규칙]
    exec_    실행·부트스트랩
    design_  디자인·문서·시각화
    review_  검증·리뷰·품질
    route_   라우팅·판단
    state_   상태 저장·복구
    mcp_     MCP 설치·연결

  [Codex 연동]
    AGENTS.md           Codex 루트 지시서
    .codex/config.toml  Codex MCP 설정
    plugins/*/codex/    플러그인별 Codex 지시서

  [커스텀 플러그인 추가]
    plugins/내이름/ 폴더 생성 → commands/*.md 추가
    파일명 = 슬래시 커맨드명 (daily-push.md → /daily-push)

  [커맨드 만드는 법]
    1. plugins/my-plugin/commands/ 폴더에 .md 파일 생성
    2. 파일명이 슬래시 커맨드명이 됨 (daily-push.md → /daily-push)
    3. 형식:

    ─────────────────────────────
    ---
    description: "커맨드 설명 (슬래시 메뉴에 표시됨)"
    allowed-tools: Bash(powershell:*), Bash(where:*)
    ---

    ## Context
    - 현재 브랜치: !`git branch --show-current`
    - 변경 파일 수: !`git status --short | wc -l`

    ## Your task

    Claude에게 시킬 내용을 한국어로 세세하게 작성.
    조건 분기, 반복, 외부 명령 실행 모두 가능.

    예:
    1. 변경사항 있으면 git add -A && git commit -m "자동 커밋"
    2. 원격에 push
    3. Slack으로 알림 (Slack MCP 설치 시)
    ─────────────────────────────

  [Context 실시간 주입 (!`명령어`)]
    !`git log --oneline -5`          → 최근 커밋 5개 주입
    !`cat .env.example`              → 파일 내용 주입
    !`where codex-auto 2>nul && echo YES || echo NO`  → 가용 여부 주입

  [allowed-tools 목록]
    Bash(git:*)          git 명령
    Bash(powershell:*)   PowerShell
    Bash(where:*)        프로그램 경로 확인
    Bash(npm:*)          npm 명령
    Bash(claude:*)       claude mcp add 등
    Edit                 파일 편집
    Write                파일 생성
    Read                 파일 읽기
    WebSearch            웹 검색 (내장)

  [예시 커맨드 아이디어]
    /daily-report    오늘 커밋 요약 + Slack 전송
    /deploy-prod     프로덕션 배포 + 슬랙 알림
    /pr-draft        PR 초안 자동 생성
    /db-backup       DB 백업 + 결과 보고
    /perf-check      성능 측정 + 리포트 생성
    /translate-ko    선택 파일 한국어 번역

  [기존 방식 (레거시)]
    .claude/commands/ 에 직접 추가해도 됨
    plugins/ 는 좀 더 구조화된 방식


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  13. Loading Order (전체 목록)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  .claude/hooks/hook-00-init.md
  .claude/skills/skill-01-research.md ~ skill-38-token-watchdog.md
  .claude/skills/exec_orca-auto.md
  .claude/skills/state_session.md
  .claude/skills/route_dispatch.md
  .claude/hooks/hook-01~08 (pre-task, post-impl, post-review, pre-deploy,
                             post-deploy, notify, layout-lock, ai-handoff)
  .claude/hooks/post-impl-verify.sh
  .claude/hooks/protect-critical-files.sh
  .claude/learning/optimization-rules.json
  .claude/learning/failure-patterns.json


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  14. MCP 추가 설치 (필요할 때)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  기본 설치됨: context7, playwright, thinking
  자동 연결 (claude.ai): Figma, Gamma, Gmail, Canva, Mermaid

  추가 설치 — Claude에서 슬래시 커맨드로:
    /plug_dev      GitHub, Docker, AWS, Firebase, Vercel...
    /plug_data     MySQL, MongoDB, BigQuery, Sheets...
    /plug_design   Canva, Figma, Gamma, PowerPoint...
    /plug_collab   Slack, Notion, Jira, Gmail...
    /plug_web      Playwright, Puppeteer, Apify...
    /plug_docs     PDF, DOCX, OCR
    /plug_media    Whisper, TTS, FFmpeg
    /plug_all      전체 한 번에


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  15. 플러그인 생태계 (2026-04-19 업데이트)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  [구조 원칙]
    plugins/<name>/         ← 소스 오브 트루스 (편집은 여기만)
    .claude/commands,skills ← sync 결과물 (자동 생성)
    sync-plugins.sh         ← plugins/ → .claude/ 단방향

  [플러그인 — 14개 완성 + 3개 스펙]
    ● exec_  (실행 계열)
        exec_orch            멀티AI 오케스트레이션 엔진 (코어)
        exec_claude          Claude 깊이 활용 — 구조화 질문·아티팩트·커넥터·확장 사고 (NEW 2026-04-23)
        exec_learning        세션 학습·실패 패턴
        exec_session_guard   스냅샷 자동 저장
        exec_voice           음성 STT·TTS·회의록

    ● mcp_  (MCP 설치 허브)
        mcp_dev              GitHub·Docker·AWS·Firebase
        mcp_collab           Slack·Notion·Jira·Gmail
        mcp_data             MySQL·MongoDB·BigQuery
        mcp_docs             PDF·DOCX·OCR
        mcp_media            Whisper·TTS·FFmpeg
        mcp_web              Playwright·Puppeteer·Apify

    ● design_  (문서 생성)
        design_excel         Excel (openpyxl·차트)
        design_ppt           PPT (Canva·Figma·Gamma)
        design_word          Word (python-docx·Mermaid)

    ● review_
        review_qa            코드 리뷰·보안·품질·테스트

    ● Phase 1 스펙 (status=spec-only, 플랫폼에서 구현)
        exec_scheduler       크론·DAG 워크플로우
        mcp_social           YouTube Data API v3
        cost_youtube         YouTube 수익화 (research·upload·analytics)

    ※ 전체 로드맵 (미래 26개): docs/2026-04-19/로드맵.md

  [plugin.json 표준 스키마 v1.2]
    필수: name, display, prefix, version, status, phase
    옵션: commands, skills, agents, hooks, dependencies, entry_points, metadata
    검증: python .claude/scripts/validate-plugin-schema.py

  [신규 스크립트 (v2)]
    sync-plugins.sh --dry      내용 diff + rename 미리보기
    sync-plugins.sh --check    드리프트·orphan 점검만
    sync-plugins.sh --verbose  상세 출력
    install.sh                 macOS/Linux 초기 설치
    uninstall.sh               sync 결과물 제거
    validate-plugin-schema.py  plugin.json 일괄 검증
    resolve-plugin-order.py    dependencies 위상정렬
    orca-status.sh             워커·큐·heartbeat 통합 조회

  [신규 커맨드]
    /status    오케스트레이션 통합 대시보드 (워커·큐·sync·heartbeat)
    /help      플러그인 도움말 (/help 또는 /help <plugin>)
    /help --roadmap   로드맵 요약

  [아키텍처 다이어그램 커맨드 (NEW 2026-04-23)]
    /arch-mindmap <주제>     방사형 마인드맵 1장 (Mermaid → PNG)
    /arch-layered <주제>     레이어 케이크 1페이지 (위→아래 계층)
    /arch-cheatsheet <주제>  3컬럼 치트시트 (트리+블록+코드)
    /arch-auto <주제>        주제 보고 자동 패턴 선택
    출력: outputs/arch/<pattern>-<slug>-<date>.{md,png,pptx,pdf}

  [Claude 깊이 활용 커맨드 (NEW 2026-04-23, exec_claude)]
    /claude-status           가용성 + 시나리오 안내
    /claude-ask <topic>      구조화 질문 폼 생성
    /claude-artifact         인터랙티브 HTML (단일 파일)
    /claude-connectors       SaaS 커넥터 1-click 추가
    /claude-thinking         Extended Thinking 8단계 분해

  [라우팅 고도화 (route_dispatch v2)]
    AI별 특성 매트릭스 반영:
      Claude Opus   → 설계·판단·복잡 추론 (단가 높음 — 결정만)
      Claude Sonnet → 일반 구현·보완 (중단가)
      Codex         → 대량 코드 500줄+ (병렬 4대)
      Gemini Flash  → 검증·리뷰·문서 (저단가, 비용 효율)
    비용 절감: 프롬프트 캐싱 + Gemini Flash 우선 검증

  [템플릿]
    plugins/_template/       새 플러그인 만들 때 복사 기준
      ├─ plugin.json         스키마 v1.2 표준 예시
      ├─ README.md           사용법 문서 템플릿
      ├─ SPEC.md             상세 스펙 (spec-only 단계)
      ├─ commands/example.md
      ├─ skills/skill-example.md
      ├─ hooks/hook-example.md
      └─ agents/agent-example.md


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  16. Design 도구 워크플로우 (R51 — 2026-04-30)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  HTML/CSS 한 번 작성 → PPT / PDF / PNG 모두 자동 출력.
  공통 디자인 시스템 (gold/sage/plum/terracotta/deep-gold) 공유.

  [3 가지 출력]
    1. PPT (PPTX)        /design_ppt   "주제" 13~40
                          → outputs/ppt-*/automation-guide.pptx 등
    2. PDF (A4/Digital)  /pdf-generate "주제" A4 또는 Digital
                          → outputs/pdf/<date>/<name>.pdf
    3. 단일 PNG          python .claude/scripts/render-screens.py
                          → docs/screens/our-{arch,func}/*.png

  [공통 디자인 시스템]
    팔레트:  gold #B8864E · sage #6B8E7F · plum #7A4E6B
              terracotta #B25A3E · deep-gold #8A6235
    폰트:    Pretendard Variable (한글) + JetBrains Mono (코드) + Fraunces (큰 제목)
    공통 CSS: docs/screens/our-html/_styles.css (또는 outputs/ppt-*/styles/design-system.css)

  [차트 / SVG / 카드 그리드 4 패턴 (skill-ppt-pitfalls § 13)]
    A. BEFORE/AFTER 막대 차트   비용·성능 비교 (예: −92%)
    B. KPI sparkline             4 KPI 트렌드 (24h 미니 라인)
    C. timeline 막대 + 그라디언트  단계적 backoff·escalation
    D. SVG 다이어그램             gradient defs + marker 화살표 + 카테고리 색
    E. 표 → 카드 그리드           8행+ 표 가독성 떨어질 때
    F. 결정 트리 배너             분기 시각화 (sage YES / terracotta NO)

  [Screens 워크플로우 (단일 이미지)]
    docs/screens/our-html/<name>.html
                      ↓  prefix 자동 분류
                      ├─ arch-*.html → docs/screens/our-arch/<name>.png
                      └─ func-*.html → docs/screens/our-func/<name>.png

    렌더:
      python .claude/scripts/render-screens.py              # all
      python .claude/scripts/render-screens.py arch-system  # one

    활용:
      our-arch/arch-system-overview.png  → README hero
      our-func/func-cost-saving.png      → 마케팅 / 트윗
      our-arch/arch-plugin-ecosystem.png → 플러그인 카탈로그 표지

  [PDF 신규 — design_pdf v0.2]
    /pdf-generate  HTML/CSS → Playwright → PDF
                   A4 (794×1123) · Letter (816×1056) · Digital (1920×1080)
                   print_background: true 필수 (배경 그라디언트)
                   wait_for_timeout(900~1500) (폰트·아이콘 로드)
    /pdf-fill      PDF 양식(form) 자동 채우기
    /pdf-sign      전자서명·직인 삽입
    /pdf-secure    암호화·워터마크
    skill: skill-pdf-design-system (R51 신규) — 12 계명 + 함정 5 가지

  [핵심 원칙]
    - design_ppt / design_pdf / Screens 모두 동일 시각 톤
    - 표 8행+ 또는 폰트 16px 이하면 → 카드 그리드 변환
    - 차트 막대 width 는 데이터 비율로 직접 계산 (예: width: 8.3%)
    - SVG `<defs>` 에 gradient + marker 미리 정의
    - 출력 후 PIL overflow 검증 + Read tool 로 OCR 직접 확인

  상세: plugins/design_ppt/skills/skill-ppt-pitfalls.md § 13~14
        plugins/design_pdf/skills/skill-pdf-design-system.md
        .claude/scripts/render-ppt-multi.py / render-screens.py


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  17. 추가 사항 요약
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  2026-05-11 안정성 + zero-touch 자동화 누적 (commit 11건):
    ✓ Watchdog 메모리 누수·자살 로직 보강 (24371f0)
       - Claude 세션 stale 시 watchdog 가 자살하던 로직 제거 (워커 유지)
       - RSS+자식 합산 임계 모니터링 (default 2GB, --max-rss-mb)
       - max-uptime 정책 (default 6h)
       - psutil → tasklist → ps fallback
       - kill_worker_tree (taskkill /T /F + killpg)
       - 로그 백업 5개 회전 유지
       - orca-status.sh task-instruction.md 템플릿 제외 카운팅
    ✓ Hook 4개 추가 등록 (67664ff, b127d2b)
       - PreToolUse Bash: block_dangerous_bash.py, log_bash_command.py
       - PostToolUse Edit|Write: post_write_pycheck.py, markdown_formatter.py
       - OS 인식 + 따옴표 안 자연어 무시 (commit msg false positive 해결)
       - Windows cp949 인코딩 자동 회피 (UTF-8 강제)
    ✓ Agents sync 추가 (aebdffe)
       - sync-plugins.sh 가 commands/skills 만 처리하던 것 → agents 포함
       - 누락 4개 복구: agent-01-judge, agent-07-code-reviewer, 08-test-runner, 09-explorer
       - 동명 6 그룹 (총 27 인스턴스) md5 동일 — idempotent 덮어쓰기
    ✓ External Watchdog (OS-level, Task Scheduler 1분 간격) (247b3e9, c22ab03, 72bc1cc)
       - VSCode hang 외부 감지 (heartbeat 5분 정지 + VSCode 살아있음)
       - SessionStart hook 으로 idempotent 자동 등록 (사용자 액션 0)
       - VBS wrapper + Task Hidden 속성 → cmd 창 깜빡임 제거
       - WATCHDOG_AUTO_RESTART=1 옵션 (강제 재시작, 디폴트 off)
       - WATCHDOG_NOTIFY=1 옵션 (BurntToast 알림)
    ✓ 협업 원칙 5중 박기 (247b3e9, fef1338, 330187e)
       - 전수조사 의무 (전수조사·분석·실행·확인·보고 5단계)
       - Zero-touch 자동화 (사용자 액션 요구 X, 알림은 크리티컬 5가지만)
       - 하드 경로 금지 (사용자명·Python 버전·OS 절대경로 X, 동적 검색)
       - Template kit 원칙 (orchestration_v1 = 공통 배포 kit, ~/.claude/ 직접 X)
       - 박힌 위치: memory + CLAUDE.md + rules/{failure-mode,best-practices} + hook-00-init.sh
    ✓ Global CLAUDE.md template + install 배포 (506216b)
       - setup/templates/global-CLAUDE.md (3원칙 + 4번째 template kit 원칙)
       - setup/modules/03-settings.bat 가 ~/.claude/CLAUDE.md 로 배포
       - 기존 파일 .bak 백업 후 덮어쓰기
    ✓ 하드 경로 wrapper 패턴 (330187e)
       - audit-*.py 3개: C:/Users/ja205/AppData/Local/Temp/ → tempfile.gettempdir()
       - external-watchdog Task Scheduler: python 풀패스 박는 대신 wrapper.bat
         wrapper 가 매번 where python / py launcher / registry 순 동적 검색
       - setup.iss 주석 placeholder 화

  2026-04-30 R51~R71 누적 (43 슬라이드 PPT × 3종):
    ✓ 차트·SVG·카드 그리드 4 패턴 표준화 (skill § 13)
    ✓ Screens 워크플로우 신규 (docs/screens/our-html → PNG)
    ✓ design_pdf v0.2 — 디자인 시스템 공유 + 12 계명 신규
    ✓ render-ppt-multi.py — 3 PPT 통합 렌더 스크립트
    ✓ render-screens.py — 단일 PNG 자동 분류 (arch-/func-)
    ✓ 어려운 영어 술어 → 쉬운 한글
       (24/7 → 쉬는 시간 없음, Backoff → 한도 대기, Best Practices → 운영 팁,
        Watchdog → 감시자, Heartbeat → 살아있나 신호, Caching → 캐시)
    ✓ 본문 폰트 16→18px, TIP desc 14→18px (가독성 ↑)
    ✓ TOC PART 이름 모두 한글화 (3 PPT)
    ✓ Cover stat label 모두 한글화
    ✓ 단일 *-guide.pptx 로 통합 (_디자인적용 suffix 삭제)
    ✓ PIL 43/43 PASS

  2026-04-19 커밋 누적:
    ✓ 14개 허브 커맨드 description 한글 통일
    ✓ 13개 orphan 허브 → plugins/ SoT 이동
    ✓ Bash 권한 28개 allow (프롬프트 최소화)
    ✓ plugins/_template/ 표준 구조
    ✓ plugin.json 스키마 v1.2 (14개 업그레이드)
    ✓ .claude-plugin/marketplace.json (Claude 플러그인 마켓 형식)
    ✓ sync-plugins.sh v2 (orphan·drift·dependency 순서)
    ✓ validate-plugin-schema.py (필수 필드·타입 검증)
    ✓ install.sh / uninstall.sh (Unix 계열)
    ✓ Phase 1 스펙 3개 (spec-only)
    ✓ 14개 README.md 자동 생성
    ✓ docs/2026-04-19/: 플러그인.txt + 로드맵.md
    ✓ prefix 13개 신규 등록 (cost_/perf_/growth_/ai_/sec_/infra_/...)
    ✓ route_dispatch v2 (AI 단가·특성 매트릭스)
    ✓ /status, /help 커맨드 추가


============================================================
  GitHub: https://github.com/bernakilljos/orchestration
  Release: https://github.com/bernakilljos/orchestration/releases
============================================================
