---
description: "Word 생성 (python-docx + Mermaid + PDF)"
---

# /design_word — Word 생성 허브

## 포함 커맨드
- `/word-make` — Word 파일 생성 (**기본 액션**)
- `/word-status` — 생성 상태 확인

## 기본 실행
`/word-make` — .docx 생성 (다이어그램 포함 시 PDF 변환 가능).
